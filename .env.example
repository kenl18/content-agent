# Optional. Only used by createAnthropicProvider()/createContentClient({ apiKey }) — the Anthropic
# Console (pay-as-you-go) path. Scheduled EmailOps composition no longer reads it (ADR-0017: the
# Claude Code subscription provider uses the machine's `claude auth login`, not an API key).
ANTHROPIC_API_KEY=
